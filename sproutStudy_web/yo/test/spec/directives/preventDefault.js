'use strict';

describe('Directive: preventDefault', function () {
  beforeEach(module('sproutStudyApp'));

  var element;

//  it('should make hidden element visible', inject(function ($rootScope, $compile) {
//    element = angular.element('<prevent-default></prevent-default>');
//    element = $compile(element)($rootScope);
//    expect(element.text()).toBe('this is the preventDefault directive');
//  }));
});
